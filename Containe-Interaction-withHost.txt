Execute a command inside a running contgainer 
-> docker exec -it <container_id> 
mkdir data 
echo "Hello Docker

✅ Create Network
docker network create my-network
✅ Run Containers on Same Network
# Server container
docker run -d --name server --network my-network nginx

# Java container
docker run -d --name java-app --network my-network java-app

docker run -d -p 8080:8080 java-app